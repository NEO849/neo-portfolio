# OSINT-API — Laufzeit-Abhängigkeiten
# Pins als untere Grenze (kompatibel zu aktuell getesteten Versionen).

# Web-Framework + Server
fastapi>=0.135,<1.0
uvicorn[standard]>=0.48,<1.0

# Rate-Limiting + Validierung
slowapi>=0.1.9,<0.2
pydantic>=2.13,<3.0

# HTTP-Client (async)
httpx>=0.28,<1.0

# DNS / WHOIS
dnspython>=2.6,<3.0
python-whois>=0.9.6,<1.0

# Telefonnummern
phonenumbers>=9.0,<10.0

# Bild-Analyse (EXIF + Perceptual Hash)
Pillow>=12.0,<13.0
ImageHash>=4.3,<5.0
c2pa-python>=0.34,<1.0   # Content Credentials (Bild-Herkunft/Authentizität) — optional, graceful
defusedxml>=0.7   # sicheres XMP-Parsing (Pillow getxmp) — KI-Herkunft/Bearbeitungs-Historie
