# Entwicklungs-/Test-Abhängigkeiten (nicht für Produktion nötig).
# Die Tests laufen auch ohne pytest standalone:
#   python3 -m tests.test_netz_schutz
-r requirements.txt
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
